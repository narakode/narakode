---
interface Props {
    title: string
    description: string
}

const { title, description } = Astro.props
---

<section class="space-y-6">
    <div class="flex items-center justify-between">
        <div class="space-y-2">
            <h2 class="font-bold text-4xl text-gray-900">{title}</h2>
            <p class="text-lg text-gray-700">{description}</p>
        </div>
        <slot name="action" />
    </div>

    <slot />
</section>