---
interface Props {
    class?: string
    tag?: 'div' | 'main'
}

const { class: className, tag: Element = 'div' } = Astro.props
---

<Element class={`${className} container mx-auto px-4`}>
    <slot />
</Element>