---
interface Props {
    course: {
        name: string
        level: string
        description: string
    }
}

const { course } = Astro.props
---

<article>
    <p>{course.level}</p>
    <h3>{course.name}</h3>
    <p>{course.description}</p>
</article>