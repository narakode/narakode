---
import Card from './app/Card.astro'

interface Props {
    video: {
        name: string
        url: string
    }
}

const { video } = Astro.props
---

<Card class="space-y-2">
    <iframe src={video.url} class="w-full" title={video.name}></iframe>
</Card>